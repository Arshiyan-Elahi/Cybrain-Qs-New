fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy==2.0.52
psycopg[binary]==3.3.4
alembic==1.19.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
email-validator==2.3.0
httpx==0.28.1
pgvector==0.5.0
python-docx==1.2.0
pypdf==6.16.1
# Optional production PDF/OCR stack (heavy; not required for core ingest):
#   pip install docling
# Docling is auto-detected when present; pypdf remains the default fallback.
# OCR engines are pluggable via app.processing.ocr (NullOcrProvider by default).
# Local embedding fallback (auto-routing when remote Nomic is offline).
# transformers>=5.5 ships native NomicBert; Hub auto_map for
# nomic-ai/nomic-embed-text-v1.5 still calls get_extended_attention_mask
# (removed in transformers 5) when trust_remote_code executes remote code.
sentence-transformers>=6.0.0,<7
transformers>=5.5.0,<6
einops>=0.8.0
pytest==9.1.1
reportlab==4.4.4  # PDF fixtures for ingestion regression tests only
